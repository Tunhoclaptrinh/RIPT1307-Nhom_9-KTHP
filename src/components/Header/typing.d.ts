export type HeaderProps = {
	// subTitle of the header
	subTitle?: string;
	// title of the header => các nút truyền vào header
	button?: JSX.Element[];
	// title of the header => các danh mục truyền vào header
	menu?: JSX.Element[];
};
